{
  "name": "devops-login-page",
  "version": "1.0.0",
  "description": "Página de login simples com deploy automatizado",
  "main": "index.html",
  "scripts": {
    "build": "npm run minify",
    "minify": "html-minifier-terser --input-dir . --output-dir dist --file-ext html --minify-css --minify-js --remove-comments --collapse-whitespace",
    "serve": "npx serve dist",
    "dev": "npx serve ."
  },
  "keywords": [
    "html",
    "css",
    "login",
    "static"
  ],
  "author": "Danillo",
  "license": "MIT",
  "devDependencies": {
    "html-minifier-terser": "^7.2.0",
    "serve": "^14.2.1"
  }
}
